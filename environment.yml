name: nc2zarr

channels:
  - conda-forge

dependencies:
  - python >=3.7
  # Production
  - click
  - dask
  - fsspec
  - netcdf4
  - numpy
  - pandas
  - pyyaml
  - s3fs
  - xarray
  - zarr
  # Testing
  - pytest
  - pytest-cov
